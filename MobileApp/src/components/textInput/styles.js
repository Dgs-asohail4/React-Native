import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    searchSection: {
        flexDirection: 'row',
       
        margin:20
    },
    searchIcon: {   
        alignItems : 'center',
        padding:15,
    },


});

export default styles;